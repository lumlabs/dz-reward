[project]
name = "dz-reward"
version = "0.3.0"
description = "Validator debt management tool for DoubleZero network"
readme = "README.md"
requires-python = ">=3.12"
dependencies = [
    "loguru>=0.7.3",
    "requests>=2.32.5",
    "python-dotenv>=1.0.0",
]
